type Pool @entity {
  id: ID!
  pid: BigInt!
  lpToken: Bytes!
  token: Bytes!
  gauge: Bytes!
  balRewards: Bytes!
  stash: Bytes! 
  shutdown: Boolean!
}

type User @entity {
  id: ID!
  pools: [Pool!]
}